{% extends admin.getTemplate('base_list_field') %}

{% block field%}
    {%- if value is empty -%}
        &nbsp;
    {%- else -%}
        <span class="label label-default order-status status-{{ value }}">{{ value }}</span>
    {%- endif -%}
{% endblock %}