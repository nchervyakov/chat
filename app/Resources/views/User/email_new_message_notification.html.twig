{% extends ':Email:email_base.html.twig' %}

{% block content_html %}
    <p>Hi, {{ user.fullName }}!</p>
    <p><a href="{{ url('user_show', {'user_id': companion.id}) }}">{{ companion.fullName }}</a> has sent you a message. You can view it by clicking this link:<br>
        <a href="{{ url('chat_show', {'companion_id': companion.id}) }}">{{ url('chat_show', {'companion_id': companion.id}) }}</a>
    </p>
    <p>Best regards <span class="br"><a href="{{ website }}">{{ website }}</a> team.</span></p>
{% endblock %}